# Pinned to the versions these results were produced with.
# Reproducibility is a claim this project makes explicitly; unpinned dependencies would
# make it false.
pandas==3.0.2
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
matplotlib==3.10.8
yfinance>=0.2.40
pyarrow>=15.0.0
pytest>=8.0.0
statsmodels>=0.14.0
